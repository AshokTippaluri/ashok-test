pipeline {
    agent any
    stages {
        stage('Build and Deploy') {
            steps {
                sh 'pwd
                    ls '
                // sh 'docker-compose up --build -d || error "Deploy failed"'
            }
        }
        stage('Cleanup') {
            steps {
                sh 'docker-compose down -v || error "Cleanup failed"'
            }
            
        }
    }
}
